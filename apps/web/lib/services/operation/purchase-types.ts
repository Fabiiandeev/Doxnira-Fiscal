export type PurchaseItem = { id: string; productId: string; quantity: string; receivedQuantity: string; unitCost: string; totalAmount: string; product?: { name: string; code: string } };
export type Purchase = { id: string; number: string; status: string; supplierId: string; warehouseId: string; issueDate: string; expectedDate?: string; totalAmount: string; notes?: string; supplier?: { razaoSocial?: string; nome?: string; nomeFantasia?: string }; warehouse?: { name: string }; buyer?: { name: string }; items?: PurchaseItem[]; receipts?: Array<{ id: string; receivedAt: string; items: unknown[] }>; timeline?: Array<{ id: string; action: string; createdAt: string; metadata?: unknown }> };
export type PurchaseInput = { number: string; supplierId: string; warehouseId: string; issueDate: string; expectedDate?: string | null; totalAmount: number; freightAmount: number; discountAmount: number; taxAmount: number; notes?: string; items: Array<{ productId: string; quantity: number; unitValue: number; discountAmount: number; taxAmount: number }>; installments: Array<{ number: string; dueDate: string; amount: number }>; attachments: Array<{ name: string; url: string }> };
export type OperationPage<T> = { data: T[]; pagination: { page: number; pageSize: number; total: number; totalPages: number } };
