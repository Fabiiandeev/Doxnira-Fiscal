"/**\n * @fileoverview Testes E2E para os endpoints de consulta Payable (Sprint 01). Includes cross-company isolation and filter validation.\n */\nimport { createServer } from "node:test";\nimport { api, prisma } from "../../src/server.js"; // Assumindo que 'api' é o app Express e 'prisma' é o client.\n// Mock de dependências para garantir isolamento de dados antes dos testes:\nconst setupDatabase = async (companyAId, companyBId) => {\n    await prisma.payable.deleteMany({\n        where: { \n            companyId: [companyAId, companyBId] \n        } \n    });\n    // Implementar mock de dados para garantir um estado conhecido.\n};\n\ndetail('FiscalPay Query API E2E Tests', async (t) => {\n    const COMPANY_A = 'uuid-company-a';\n    const COMPANY_B = 'uuid-company-b';\n    const USER_A_TOKEN = 'token-user-a';\n    const USER_B_TOKEN = 'token-user-b';\n
    await setupDatabase(COMPANY_A, COMPANY_B);\n
    // --- SETUP DE DADOS MOCK PARA TESTES ---\n    // Cria Payable A (Empresa A)\n    const payableAData = await prisma.payable.create({\\n        data: {\\n            companyId: COMPANY_A,\\n            supplierName: 'FORNECEDOR A LTDA',\\n            supplierCnpj: '00011122233311',\\n            installmentNumber: '001',\\n            dueDate: new Date('2026-07-15T00:00:00.000Z'),\\n            amount: 1250.5,\\n            paymentMethod: 'CARD_A',\\n            status: 'OPENED', // Status real existente\n            source: 'SYSTEM_SOURCE',\\n            paidAt: null,\\n            createdAt: new Date(),\\n            updatedAt: new Date(),\\n            nfeEntryId: 'uuid-nfe-a'\\n        },\\n        include: {\\n            nfeEntry: { create: { id: 'uuid-nfe-a', accessKey: 'AACCKEY1234567890', number: '123', series: 'A', issueDate: new Date('2026-07-10T00:00:00.000Z'), totalAmount: 1250.5, status: 'VALID', entryStatus: 'OK', financialStatus: 'COMPLETED' }},\\n            supplier: { create: { id: 'uuid-supplier-a', displayName: 'FORNECEDOR A LTDA', cnpj: '00011122233311', active: true }}\n        },\\n    });\n
    // Cria Payable B (Empresa B) - Outro status e data diferente para testar isolamento/filtro.\n    await prisma.payable.create({\\n        data: {\\n            companyId: COMPANY_B,\\n            supplierName: 'FORNECEDOR B S.A',\\n            supplierCnpj: '00022233344422',\\n            installmentNumber: '001',\\n            dueDate: new Date('2026-11-01T00:00:00.000Z'),\\n            amount: 500.0,\\n            paymentMethod: 'BANK_B',\\n            status: 'CLOSED', // Outro status real existente\n            source: 'MANUAL_IMPORT',\\n            paidAt: null,\\n            createdAt: new Date(),\\n            updatedAt: new Date(),\\n            nfeEntryId: 'uuid-nfe-b'\\n        },\\n        include: {\\n            nfeEntry: { create: { id: 'uuid-nfe-b', accessKey: 'BACCKEY1234567890', number: '999', series: 'Z', issueDate: new Date('2026-10-01T00:00:00.000Z'), totalAmount: 500.0, status: 'INVALID', entryStatus: 'PENDING', financialStatus: 'PROCESSING' }},\\n            supplier: { create: { id: 'uuid-supplier-b', displayName: 'FORNECEDOR B S.A', cnpj: '00022233344422', active: true }}\n        },\\n    });\n
    // ------------------------------------------------------------\n    // TESTE DE SEGURANÇA E AUTENTICAÇÃO (CROSS-COMPANY ISOLATION)\n    // ------------------------------------------------------------\n    t.test('Deve retornar 401 se não houver token de autenticação', async () => {\n        const response = await api.get('/api/companies/dummy').set('Authorization', 'Bearer dummy');\n        expect(response.status).toBe(401); // Esperado: Falha na autenticação.\n    });\n
    t.test('Deve garantir isolamento de dados (User A só acessa Company A)', async () => {\n        // Simula acesso como User A (Company A) tentando ver o payable da Company B.\n        const response = await api.get('/api/companies/' + COMPANY_A + '/fiscal-pay/payables')\n            .set('Authorization', `Bearer ${USER_A_TOKEN}`);\n        // Deve retornar os dados de Payable A e NADA de Payable B.\n        expect(response.status).toBe(200);\n        const data = response.body.data;\n        expect(data.length).toBeGreaterThanOrEqual(1); // Pelo menos payable A deve estar lá.\n        // Valida que todos os itens pertencem a Company A (O filtro companyId na rota garante isso, mas é bom testar o resultado).\n        const allCompanyIds = data.map(d => d.companyId).every(id => id === COMPANY_A);\n        expect(allCompanyIds).toBe(true);\n    });\n\n    // ------------------------------------------------------------\n    // TESTE DE CONSULTA PAGABLES (GET /payables) ---\n    // ------------------------------------------------------------\n    describe('Listing Payables', () => {\n        test('Deve retornar paginacao correta e aplicar filtros com sucesso', async () => {\n            const response = await api.get('/api/companies/' + COMPANY_A + '/fiscal-pay/payables')\n                .set('Authorization', `Bearer ${USER_A_TOKEN}`) \\n                .query({ page: 1, pageSize: 20, status: 'OPENED' });\n            \n            expect(response.status).toBe(200);\n            const data = response.body.data;\n            // Verifica paginação:\n            expect(response.body.pagination.total).toBeGreaterThanOrEqual(1);\n            // Verifica que os dados retornados correspondem aos filtros aplicados.\n            const matchedStatus = data.every(d => d.status === 'OPENED');\n            expect(matchedStatus).toBe(true);\n        });\n
        test('Deve permitir busca textual por CNPJ e NFeNumber (Query q)', async () => {\n            // Busca pelo fornecedor B, que está em outra empresa (deve falhar o isolamento)\n            const response = await api.get('/api/companies/' + COMPANY_A + '/fiscal-pay/payables')\n                .set('Authorization', `Bearer ${USER_A_TOKEN}`) \\n                .query({ q: '00022233344422' }); // CNPJ de Empresa B.\n            \n            expect(response.status).toBe(200);\n            const data = response.body.data;\n            // Dado o isolamento, não deve encontrar nada que corresponda a um outro companyId.\n            expect(data.length).toBe(0); // Se houver algum retorno, é falha de segurança (Cross-Company Leakage).\n        });\n
        test('Deve aplicar ordenacao e filtros monetarios corretamente', async () => {\n             // Configura dados temporários para testar min/max amount.\n            const payableSmallAmount = await prisma.payable.create({\\n                data: {\\n                    companyId: COMPANY_A,\\n                    supplierName: 'TEST MIN',\\n                    supplierCnpj: '00000000000000',\\n                    installmentNumber: '1',\\n                    dueDate: new Date('2026-07-15T00:00:00.000Z'),\\n                    amount: 10,\\n                    paymentMethod: 'CARD_A',\\n                    status: 'OPENED',\\n                    source: 'SYSTEM_SOURCE',\\n                    paidAt: null,\\n                    createdAt: new Date(),\\n                    updatedAt: new Date(),\\n                    nfeEntryId: 'uuid-test'\\n                },\\n                include: {\\n                    supplier: { create: { id: 'uuid-s1', displayName: 'TEST MIN', cnpj: '00000000000000', active: true }}\n                }  \\n            });\n
            const response = await api.get('/api/companies/' + COMPANY_A + '/fiscal-pay/payables')\n                .set('Authorization', `Bearer ${USER_A_TOKEN}`) \\n                .query({ minAmount: 5, maxAmount: 15, sortBy: 'amount', sortOrder: 'asc' });\n            \n            expect(response.status).toBe(200);\n            const data = response.body.data;\n            // Verifica se o filtro monetário funcionou (deve retornar apenas este dado)\n            expect(data[0].amount).toEqual(10);\n        });\n    });\n\n    // ------------------------------------------------------------\n    // TESTE DE RESUMO AGREGADO (GET /summary) ---\n    // ------------------------------------------------------------\n    describe('Listing Summary', () => {\n        test('Deve retornar totais corretos e agrupar por status existente', async () => {\n            const response = await api.get('/api/companies/' + COMPANY_A + '/fiscal-pay/summary')\n                .set('Authorization', `Bearer ${USER_A_TOKEN}`);\n
            expect(response.status).toBe(200);\n            const summary = response.body;\n            // Verifica os totais (baseado no payable A e possíveis mocks de teste)\n            expect(summary.total.count).toBeGreaterThanOrEqual(1);\n            expect(typeof summary.total.amount).toBe('number');\n
            // Verifica se o array byStatus foi populado com status existentes:\n            const foundStatuses = response.body.byStatus.map(item => item.status);\n            expect(foundStatuses).toContain('OPENED'); // Status de Payable A.\n            // Se adicionarmos outro mock, ele deve aparecer aqui.\n        });\n
        test('Deve retornar zeros e arrays vazios para empresa sem contas', async () => {\n             const NO_CONTENT_COMPANY = 'uuid-empty';\n             await prisma.payable.create({ data: { companyId: NO_CONTENT_COMPANY, supplierName: 'EMPTY' } }); // Garantir que o companyId existe na base mas sem dados de Payable.\n
             const response = await api.get('/api/companies/' + NO_CONTENT_COMPANY + '/fiscal-pay/summary')\n                .set('Authorization', `Bearer ${USER_A_TOKEN}`); \\n            \n            expect(response.status).toBe(200);\n            const summary = response.body;\n            // Deve retornar contagens e somas zeradas.\n            expect(summary.total.count).toEqual(0);\n            expect(summary.total.amount).toEqual(0);\n            // byStatus deve ser um array vazio, pois não há dados para agrupar.\n            expect(summary.byStatus).toEqual([]);\n            // Dates devem ser null.\n            expect(summary.dueDates.earliest).toBeNull();\n        });\n    });\n});"