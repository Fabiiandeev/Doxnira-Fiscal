import assert from"node:assert/strict";import fs from"node:fs";import test from"node:test";import{adsMetrics,margin,pricing}from"../../src/modules/commerce/commerce-calculations.js";
test("receita líquida, lucro e margem incluem Ads e todos os custos",()=>{const x=margin({items:1000,freightReceived:50,discounts:20,refunds:0,chargebacks:0,productCost:400,commission:100,fixedFee:20,freightPaid:30,taxes:80,packaging:10,ads:60,otherCosts:0});assert.equal(x.netRevenue,1030);assert.equal(x.netProfit,330);assert.equal(x.marginPercent,32.03)});
test("margem negativa não é ocultada",()=>{const x=margin({items:100,freightReceived:0,discounts:0,refunds:0,chargebacks:0,productCost:120,commission:10,fixedFee:0,freightPaid:0,taxes:0,packaging:0,ads:5,otherCosts:0});assert.equal(x.classification,"NEGATIVE");assert.equal(x.netProfit,-35)});
test("preço calcula equilíbrio e recomendado com centavos determinísticos",()=>{const x=pricing({productCost:50,averageCost:0,packaging:2,commissionRate:15,fixedFee:3,freight:5,subsidy:0,taxRate:8,financialCost:0,ads:4,discount:0,desiredMargin:20});assert.ok(x.recommendedPrice>x.breakEvenPrice);assert.match(x.formula,/custos fixos/)});
test("ROAS e ACOS respeitam indisponibilidade de receita",()=>{assert.deepEqual(adsMetrics(100,500,10),{roas:5,acos:20,costPerOrder:10});assert.equal(adsMetrics(100,null,0).roas,null)});
test("migration Commerce é aditiva",()=>assert.doesNotMatch(fs.readFileSync(new URL("../../prisma/migrations/20260728050000_add_commerce_analytics/migration.sql",import.meta.url),"utf8"),/DROP TABLE/i));
