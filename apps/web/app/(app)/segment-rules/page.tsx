import { SegmentRulesView } from "@/components/segment-rules/segment-rules-view";

export const metadata = { title: "Pacotes por Segmento" };

export default function SegmentRulesPage() {
  return <SegmentRulesView />;
}
