import { TaxForecastView } from "@/components/tax-forecast/tax-forecast-view";

export const metadata = { title: "Previsao de Impostos" };

export default function TaxForecastPage() {
  return <TaxForecastView />;
}
