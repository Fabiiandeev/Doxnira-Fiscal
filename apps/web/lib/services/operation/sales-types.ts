import type { OperationPage } from "./purchase-types";
export type SalesItem = { id: string; productId: string; quantity: string; reservedQuantity: string; invoicedQuantity: string; returnedQuantity: string; unitPrice: string; product?: { name: string; code: string } };
export type Sale = { id: string; number: string; origin: string; status: string; externalKey?: string; clientId: string; warehouseId: string; issueDate: string; totalAmount: string; notes?: string; client?: { razaoSocial?: string; nome?: string; nomeFantasia?: string }; warehouse?: { name: string }; seller?: { name: string }; items?: SalesItem[]; invoices?: Array<{ id: string; status: string; documentType: string; totalAmount: string }>; returns?: Array<{ id: string; reason: string }>; timeline?: Array<{ id: string; action: string; createdAt: string }> };
export type SalesInput = { number: string; origin: string; externalKey?: string | null; clientId: string; warehouseId: string; issueDate: string; totalAmount: number; freightAmount: number; discountAmount: number; taxAmount: number; notes?: string; items: Array<{ productId: string; quantity: number; unitValue: number; discountAmount: number; taxAmount: number }> };
export type SalesPage = OperationPage<Sale>;
